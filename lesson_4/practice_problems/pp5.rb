hash = { a: 'ant', b: 'bear'}
hash.shift

# this will return the first element in a hash into a two item array
# shift destructively removes the first key-value pair in hash
# and returns it as a two-item array